# Ejercicio 2

# Constantes de color
reset = "\033[0m"
green = "\033[32m"
cyan = "\033[36m"
magenta = "\033[35m"

# Funcion para imprimir la cabecera con colores
def imprimir_cabecera():
    print(magenta+ "=" * 20 + reset)

# Función para la cabecera / pie
def imprimir_rayas():
    print("=" * 20)

# Se pide un numero al usuario——input siempre devuelve String
num = int(input("Ingrese un numero: "))
print(cyan + f"Tabla de multiplicar del {num}:" + reset)

# Imprimir la cabecera
imprimir_cabecera()

# Crear bucle del 1 al 10
for i in range(1, 11):
    # Mostrar el resultado de cada multiplicación
    print(f"{num} x {i} = {num * i}")

# Imprimir el pie
imprimir_cabecera()
