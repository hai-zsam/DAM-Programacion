# texto = input("Digite um texto: ")
# longitud = len(texto)

# def imprimir_rayitas():
#     print("-" * (longitud + 4))
    
# imprimir_rayitas()
# print("| " + texto + " |")
# imprimir_rayitas()

try:
    numero = -1
    while numero != 0:
        numero = int(input("Introduce un número: "))
        if numero < 0:
            print("El número no puede ser negativo") #raise ValueError
            continue

        suma = 0
        for i in range(1, numero + 1):
            suma += i
        print(suma)
except ValueError:
    print("El número no puede ser negativo.")
